// import { lazy } from 'react';
import {
  HashRouter as Router,
} from 'react-router-dom';

import Docusaurus from 'docusaurus';

import './App.css';

const App = () => {
  return (
    <Router>
      <Docusaurus />
    </Router>
  );
};

export default App;
